<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize Your theme here. -->
        <item name="colorPrimary">@color/clickedBtntext</item>
        <item name="colorPrimaryDark">@color/clickedBtntext</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:itemTextAppearance">@style/myCustomMenuTextAppearance</item>      // this style for change the text color of popup menu item that define below
    </style>


    <style name="CustomShapeAppearance">
        <item name="neumorph_cornerFamily">rounded</item>
        <item name="neumorph_cornerSize">16dp</item>

        <!-- Or if wants different radii depending on the corner. -->
        <!--        <item name="neumorph_cornerSizeTopLeft">16dp</item>-->
        <!--        <item name="neumorph_cornerSizeTopRight">16dp</item>-->
        <!--        <item name="neumorph_cornerSizeBottomLeft">16dp</item>-->
        <!--        <item name="neumorph_cornerSizeBottomRight">16dp</item>-->
    </style>

    <style name="CustomShapeAppearanceRecordingItem">
        <item name="neumorph_cornerFamily">rounded</item>
        <item name="neumorph_cornerSize">8dp</item>
    </style>

    <style name="MyRadioButton" parent="Theme.AppCompat.Light">
        <item name="colorControlActivated">@color/clickedBtntext</item>
        <item name="colorControlNormal">@color/unclickedBtntext</item>
    </style>

    // this code for change the text color of popup menu item
    // after this add it on app theme above
    <style name="myCustomMenuTextAppearance" parent="@android:style/TextAppearance.Widget.IconMenu.Item">
        <item name="android:textColor">@color/recordingTitle</item>
    </style>

</resources>